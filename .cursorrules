- Python 3.10+ 문법 사용
- 모든 함수에 타입 힌트 필수
- docstring은 Google 스타일로 작성
- 에러 핸들링은 try-except로 명시적 처리
- 테스트 파일은 pytest 사용
- 모든 TTS 모델은 BaseTTS 추상 클래스를 상속
- 성능 측정은 PerformanceMonitor 컨텍스트 매니저 사용
- 파일명은 snake_case, 클래스명은 PascalCase
